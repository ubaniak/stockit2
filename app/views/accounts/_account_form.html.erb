<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :balance %><br>
    <%= f.text_field :balance %>
  </div>
  <div class="field">
    <%= f.label :account_type_id %><br>
    <%= f.select :account_type_id, options_from_collection_for_select(AccountType.all, "id","name") %>
  </div>
  <div class="field">
    <%= f.label :default_sales %><br>
    <%= f.check_box :default_sales %>
  </div>
  <div class="field">
    <%= f.label :credit %><br>
    <%= f.check_box :credit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
